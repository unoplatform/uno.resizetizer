# uno.resizetizer Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-14

## Active Technologies

- C# (.NET Standard 2.0 for the MSBuild tasks assembly; build-time only — consuming apps target .NET 8/9 for Android, iOS, WASM) + Microsoft.Build.Framework / Microsoft.Build.Utilities.Core (MSBuild task API), SkiaSharp (image rasterization / color parsing), System.Xml / System.Text.Json (artifact emission). No new runtime dependencies. (main)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

# Add commands for C# (.NET Standard 2.0 for the MSBuild tasks assembly; build-time only — consuming apps target .NET 8/9 for Android, iOS, WASM)

## Code Style

C# (.NET Standard 2.0 for the MSBuild tasks assembly; build-time only — consuming apps target .NET 8/9 for Android, iOS, WASM): Follow standard conventions

## Recent Changes

- main: Added C# (.NET Standard 2.0 for the MSBuild tasks assembly; build-time only — consuming apps target .NET 8/9 for Android, iOS, WASM) + Microsoft.Build.Framework / Microsoft.Build.Utilities.Core (MSBuild task API), SkiaSharp (image rasterization / color parsing), System.Xml / System.Text.Json (artifact emission). No new runtime dependencies.

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
